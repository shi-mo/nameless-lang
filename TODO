TODO LIST
---------

[Functionality]
  GC
  *done* nls_grab(reference count)
  *done* leak check

[Refactoring]
  node type specific operation
  *done* macro:NLS_GLOBAL
  *done* add prefix:nls_sys for global symbols

[Language Specification]
  Suppress SEGV on syntax error
  set
  cond
  procedure/parallel
  nil terminated list
  *done* lambda abstraction
  *done* symbol table
  *done* variable
  *done* change list structure (simple linked-list)

[Testing]
  Detect SEGV
  *done* Automatically add nls_init(), nls_term().
  *done* #ifdef NLS_UNITTEST

[Documentation]
  comment(function spec)
  comment(structure chart)
  commit message rule
  CODINGSTD
  COPYING
  README

[Build Environment]
  source emmbed sha1-sum
